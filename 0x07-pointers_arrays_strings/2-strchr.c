#include <stdio.h>
#include <string.h>
#include "main.h"
/**
* *_strchr - this is used for search first occurence
* @s: string to be scanned or in which to search
* @c: character searched from the string
* Return: on success 1
**/

char *_strchr(char *s, char c)
{
return (strchr(s, c));

}
