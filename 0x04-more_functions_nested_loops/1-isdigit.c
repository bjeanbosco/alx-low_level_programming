#include "main.h"

/**
*_isdigit - test whether variable is number
*@c: is parametr to be verified
*Return: 1 and returns 0 if not
**/
int _isdigit(int c)
{
if (c >= 95 && c <= 122)
return (0);
return (1);
}
