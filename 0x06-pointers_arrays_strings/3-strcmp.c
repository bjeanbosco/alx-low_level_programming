#include <string.h>
#include "main.h"
/**
* _strcmp - this is compare two strting
* @s1: string to be compared with another
* @s2: second string to be compared with the fisrt
* Return: on success 1
**/

int _strcmp(char *s1, char *s2)
{

return (strcmp(s1, s2));
}
