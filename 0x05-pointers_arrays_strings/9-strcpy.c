#include "main.h"
#include <string.h>

/**
*_strcpy - this is how we copy string to another variable
*@dest: where to be copied
*@src: srting to be copied
*Return: copied string
**/
char *_strcpy(char *dest, char *src)
{
return (strcpy(dest, src));
}
