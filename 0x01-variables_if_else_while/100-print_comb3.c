#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
int c = '01';
int d = '10';
for ( ; c < '10'; c++)
{
putchar(c);
for ( ; d <= '99'
if (c <'10' && d < '99')
{
putchar(',');
putchar(' ');
}
return (0);
}
